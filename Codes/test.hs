double x = x+x

quadruple x = double( double x)

factorial n = product [1..n]

averageInt ns = sum ns `div` length ns

last2 ns = drop(length ns - 1) ns
last21 nss = head (reverse nss)

init2 y = take (length y - 1) y



-- comments